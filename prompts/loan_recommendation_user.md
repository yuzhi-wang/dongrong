请只在下面的剩余候选产品中进行匹配和排序，并严格按照系统指令输出 JSON。不要重新执行本地硬规则准入判断，不得恢复本地已排除产品。

## 客户贷前信息

{{CUSTOMER_JSON}}

## 剩余候选产品

{{PRODUCTS_JSON}}

## 本地过滤摘要

{{LOCAL_FILTER_SUMMARY_JSON}}

## 本次推荐与返回要求

本次共有 {{CANDIDATE_COUNT}} 个候选。先判定字段是必要条件、补充信息、门槛放宽、辅助偏好还是客户需求，再按各产品最强独立路径的实际匹配程度比较。同一事实不重复奖励，达到门槛后不默认数值越高越好，不自行补出客户未表达的偏好。

最终 `top3` 数组必须恰好包含 3 个产品对象，`rank` 依次为 1、2、3。第 3 个产品对象结束后，立即用 `]` 结束数组，不再追加其他候选、备选产品或说明文字。`disclaimer` 是数组外的顶层字段。只有候选总数不足 3 个时，才返回全部候选，不得编造或重复产品来凑数。复核前三边界后，只返回规定结构的 JSON。
